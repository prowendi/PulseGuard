# 版本控制
.git
.gitignore

# 编辑器 / IDE
.idea
.vscode

# 文档与设计稿（镜像里不需要）
docs
audit

# 本地脚本（smoke 不进镜像）
scripts/smoke

# 本地生成产物
dist
*.exe
pulseguard
pulseguard.exe
smoke.exe

# 本地数据库（防止意外打包数据）
data
*.db
*.db-shm
*.db-wal

# 本地配置（含真实密钥）
config.yaml
.env

# CI / 容器构建过渡产物
.omc
.dockerignore
Dockerfile
